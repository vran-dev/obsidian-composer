{
    "name": "composer",
    "version": "1.0.0",
	"scripts": {
		"build": "node build.mjs",
		"preversion": "npm run build",
		"version": "node version-bump.mjs && git add manifest.json versions.json theme.css"
	}
}
